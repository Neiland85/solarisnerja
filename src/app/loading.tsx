export default function Loading() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="text-xs tracking-[0.3em] uppercase opacity-50 animate-pulse">
        Cargando…
      </p>
    </main>
  )
}
